services:
  db:
    image: postgres:16
    restart: always
    env_file:
      - .env
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./app_v3/db/init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  pgdata: